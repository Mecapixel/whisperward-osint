# modules/__init__.py
# Specialization namespace. Each subpackage is a specialization module built
# on the reusable investigation core (core/). The first specialization is
# child_safety. A specialization declares its signals and indicators through
# core.registry without the core importing it directly.
